{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pulsegrid.serviceAccountName" . }}
  labels:
    {{- include "pulsegrid.labels" . | nindent 4 }}
{{- end }}
